from app import models
from models import user
from models import Session


# Steps of adding user to database 
#   1) Createa User: 
#         new-user = user(email, passwordHash)
#   2) Create session
#         session = new Session()
#   3) Add user to database
#         session.add(new-user)
#         session.commit() (commit everytime you interact with the database)
# 






